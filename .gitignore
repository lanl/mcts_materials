# Local secrets/config - never commit real API keys
config.json
.env

# Python build / cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# Virtual environments
.venv/

# OS
.DS_Store

# Embargoed/large data - do not commit until public release
doscar_peaks_data_with_U.csv
high_throughput_mace_results.full.csv
*.tar.gz

# MCTS run outputs (regenerate locally with mcts-run)
mcts_results*/
results_*/
*.pkl

# Kept-aside reference material: sensitivity-sweep raw data/checkpoints
reference/sensitivity_studies/results/**/*.csv
reference/sensitivity_studies/results/**/*.json

# Environment variables (contains API keys)
.env

# Any files with "secret" or "key" in the name
*secret*
*key*.txt
